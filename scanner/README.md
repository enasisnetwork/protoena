# Enasis Network SANE Batch Scanner

## Build virtual environment
```shell
python -m venv venv
source venv/bin/activate
pip install evdev
```

## Run the prototype script
```
python -m scanner.scanner
```
